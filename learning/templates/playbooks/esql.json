{
  "schema_version":2,"id":"esql-template","version":1,"starting_view":{"app":"discover"},
  "demonstration_summary":{"title":"${var.title} complete","summary":"The walkthrough used ES|QL for “${scenario.title}”, grouped the data by ${var.field}, and interpreted the row identifying ${truth.finding} with ${truth.scope}.","checks":[{"title":"Query mode","detail":"ES|QL mode allowed the source, calculations, grouping, and ordering to be expressed in one pipeline."},{"title":"Aggregation","detail":"The query grouped on ${var.field} and calculated the comparison needed to find ${truth.finding}."},{"title":"Interpretation","detail":"The decisive row was checked for ${truth.scope} before drawing the conclusion."}],"evidence":"The ES|QL table’s grouping and decisive values support this conclusion: ${truth.conclusion}","conclusion":"${truth.conclusion}","answer":{"finding":"${truth.finding}","scope":"${truth.scope}","conclusion":"${truth.conclusion}","evidence_refs":"signal,corroboration","evidence":"The grouped ES|QL result identifies ${truth.finding} with ${truth.scope}."}},
  "goals":[
    {"id":"scope","title":"Use ES|QL for the analysis","concept":"In ES|QL mode the query selects data, and data-view field statistics are unavailable.","narration":"Switch Discover to ES|QL mode.","reasoning":"This question needs an aggregation expressed as a query pipeline.","evidence":"The editor is in ES|QL mode.","demonstration":{"narration":"Switch Discover from its data-view search mode to ES|QL for “${scenario.title}”. We need a result table that can calculate and compare groups before identifying ${truth.finding}.","reasoning":"KQL filters documents but does not express this grouped calculation. ES|QL lets one pipeline select the source data, restrict rows, calculate statistics, group on ${var.field}, and order the result.","evidence":"The query editor should now be in ES|QL mode. No finding has been established yet; this step gives us the language needed to calculate ${truth.scope} reproducibly."},"requires":[],"accepts":[{"action":"query_language_changed","validators":[{"kind":"query_language","language":"esql"}]},{"action":"esql_submitted","validators":[{"kind":"query_language","language":"esql"}]}],"reference_action":{"command":"switch_query_language","target":"kibana.query_language","arguments":{"language":"esql"}},"hints":["Use the query mode designed for tabular transformations.","Open the language selector in Discover.","Choose ES|QL; Show me can switch modes."]},
    {"id":"isolate","title":"Build the requested aggregation","concept":"WHERE narrows input; STATS calculates per-group measures; SORT makes the decisive row visible.","narration":"Run an ES|QL pipeline that groups and compares the relevant values.","reasoning":"Result semantics matter more than exact formatting.","evidence":"The table contains the expected grouping and decisive values.","demonstration":{"narration":"Enter and run this ES|QL pipeline: ${var.query}. Read it from left to right: it chooses the source, narrows the input, calculates grouped measures by ${var.field}, and orders the comparison.","reasoning":"Each pipeline stage has a distinct job. Keeping filtering before STATS prevents unrelated rows from affecting the measures, while grouping and sorting make it possible to test whether ${truth.finding} is the decisive result.","evidence":"The result table should contain one row per ${var.field} group and the values needed for ${truth.scope}. The ordering should make ${truth.finding} visible as the scenario signal."},"requires":["scope"],"accepts":[{"action":"esql_submitted","validators":[{"kind":"query_contains","fields":["FROM","STATS","${var.field}"]},{"kind":"result_assertion","assertion":"signal"}]}],"reference_action":{"command":"enter_esql","target":"kibana.esql_editor","arguments":"${var.query}"},"hints":["Build a pipeline from source selection to grouped statistics.","Use FROM, WHERE, STATS, then SORT or LIMIT.","Group on ${var.field}; Show me can run the reference pipeline."]},
    {"id":"inspect","title":"Interpret the decisive result","concept":"An analytical answer must report what the calculated values mean, not just the query text.","narration":"Select or inspect the decisive result row.","reasoning":"The row supplies the quantitative evidence for the conclusion.","evidence":"The expected comparison is present in the result.","demonstration":{"narration":"Inspect the decisive result row and read its group key together with every calculated value. We expect the key to identify ${truth.finding} and the measures to show ${truth.scope}.","reasoning":"A top-ranked name without its numbers is not enough evidence, and numbers without the group key have no owner. Reading both parts verifies that the query result supports the intended comparison rather than merely returning rows.","evidence":"The decisive row should pair ${truth.finding} with ${truth.scope}. Those concrete values corroborate the aggregation and justify the conclusion: ${truth.conclusion}"},"requires":["isolate"],"accepts":[{"action":["document_expanded","field_inspected","panel_value_selected"],"validators":[{"kind":"result_assertion","assertion":"corroboration"}]}],"reference_action":{"command":"inspect_field","target":"kibana.esql_first_result","arguments":{"field":"${var.field}"}},"hints":["Read the leading row and compare its calculated columns.","Inspect the first result in the ES|QL table.","Focus on ${var.field}; Show me can point to it."]},
    {"id":"submit","title":"Submit the comparison","concept":"Report the compared groups or periods, decisive values, and conclusion.","narration":"Record the analytical conclusion with evidence.","reasoning":"A result table becomes useful only when its implication is stated.","evidence":"The response cites the aggregation result.","requires":["isolate","inspect"],"accepts":[{"action":"answer_submitted","validators":[{"kind":"answer_submitted"}]}],"reference_action":{"command":"request_answer","target":"coach.answer","arguments":{}},"hints":["State which row or period is decisive.","Use the coach comparison form.","Include finding, scope, conclusion, and evidence references."]}
  ]
}
