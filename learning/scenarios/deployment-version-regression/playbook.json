{"schema_version":2,"id":"deployment-version-regression-playbook","extends":"discover.json","variables":{"title":"Deployment-version investigation","window":"${param.window}","field":"service.version","query":"service.name: ${param.incident.service} and service.version: *","finding_phrase":"${truth.finding}"},"goal_overrides":{"inspect":{"narration":"Add the version field as a column to compare it across the failing records.","demonstration":{"narration":"Add ${var.field} as a visible table column. This lets us compare the release value across multiple failing records and check whether ${truth.finding} consistently appears in ${truth.scope}.","reasoning":"Expanding only one document could make a coincidental version look decisive. Showing ${var.field} for every filtered row makes the pattern repeatable and distinguishes a release regression from an isolated failure.","evidence":"The added column should show ${truth.finding} across the relevant failing records. That repeated document-level pattern corroborates the query and supports the conclusion: ${truth.conclusion}"},"reference_action":{"command":"add_column","target":"kibana.field_list","arguments":{"field":"${var.field}"}},"hints":["Compare the decisive field across records rather than reading a single row.","Add ${var.field} as a column to customize the table, or open its field statistics.","Add the ${var.field} column; Show me can add the reference column."]}}}
